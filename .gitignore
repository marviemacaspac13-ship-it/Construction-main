# Secrets - these hold live Supabase keys and plaintext passwords
.env
.env.local
backend/.env
passwords.md

# Dependencies
node_modules/
backend/.venv/

# Build output
dist/
.vite/

# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Runtime data
backend/runs/
*.pt

# backend/templates/ is deliberately TRACKED. The symbol crops are project
# data, not scratch: without them every electrical and plumbing scan returns
# 422, so a fresh clone should come with a working detector.

# OS
Thumbs.db
.DS_Store

# Session documentation (generated, kept local)
CLAUDE.md
CHANGES.md
docs/
