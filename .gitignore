# Secrets - these hold live Supabase keys and plaintext passwords
.env
.env.local
backend/.env
passwords.md

# Dependencies
node_modules/
backend/.venv/

# Build output
dist/
.vite/

# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Runtime data
backend/runs/
*.pt

# backend/templates/ is deliberately TRACKED. The symbol crops are project
# data, not scratch: without them every electrical and plumbing scan returns
# 422, so a fresh clone should come with a working detector.

# OS
Thumbs.db
.DS_Store

# Session documentation (generated, kept local)
CLAUDE.md
CHANGES.md
docs/

# Third-party house-plan corpus: 2,640 US marketing floor plans used only
# by tests/test_imperial_plans.py, which skips cleanly when it is absent.
# Not ours to redistribute, and far too large for the repo. The CSV beside
# it is kept as the record of what the set was and where its ground truth
# came from.
backend/tests/fixtures/plans/Data Set/Floorplan/images/
